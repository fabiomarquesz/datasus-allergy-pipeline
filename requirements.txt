# ==============================================================
# DATASUS Allergy Pipeline — Dependências Python
# ==============================================================
# Instale em venv local (para notebooks e desenvolvimento):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# O Airflow usa esta mesma lista via _PIP_ADDITIONAL_REQUIREMENTS
# no docker-compose.yml, então mantenha tudo aqui.
# ==============================================================

pysus>=1.0.0
pandas==2.2.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
